// Modelo de produto (simples, apenas para tipagem e referência)
class Produto {
    constructor(id, nome, preco) {
        this.id = id;
        this.nome = nome;
        this.preco = preco;
    }
}

module.exports = Produto;